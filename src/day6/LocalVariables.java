package day6;

public class LocalVariables {
	public static void main(String[] args) {
		/*
		 * Local variables are variables that created inside methods or statements
		 * - They don't have default values
		 * - they cannot have access modifiers
		 *   (it is the we configure the access to this specific component outside of this class)
		 */
		
		String firstName;
		// System.out.println(firstName); // doesn't compile
	}
}
